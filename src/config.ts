const path = require('path');

const config = {
    styles:[
       path.join(__dirname,"css/boostrap.css"),
       path.join(__dirname,"css/devices.min.css"),
       path.join(__dirname, "css/react-sortable-tree.css")
    ]
}

export default config;